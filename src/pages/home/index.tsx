// import styles from './index.module.css';
// import { useHome } from './index.hooks';
// import { homeStringConstant } from './stringConstants';

const Home = () => {
    // const { state, actions, vars } = useHome();
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    );
};

export default Home;
